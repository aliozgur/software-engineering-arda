Link (or path) to a setup README plus a short “how to reproduce”
At least 15 shell commands, each with one-line why (not a bare list of names)
Repo URL + commit/tag if you have one
Explicit: no secrets/tokens committed
That you can locate a process, inspect PATH, and redirect output in a terminal