Repo: https://github.com/YOU/dotfiles  commit abc1234
README: repo/README.md — clone, run bootstrap.sh, open a new terminal, then `git --version`, `python --version`.

Commands (name — why):
1. pwd — confirm home
2. ls -la — see dotfiles
3. cd ~/dotfiles
4. git status — working tree vs index
5. echo $PATH — inspect PATH
6. which git
7. git --version
8. python --version
9. mkdir -p ~/bin
10. echo hello > /tmp/out.txt — redirect stdout
11. cat /tmp/out.txt
12. ps -ef | head — list processes
13. chmod u+x bootstrap.sh — permissions
14. export EDITOR=code — env var
15. history | tail — review what ran

No tokens or .env files in git.