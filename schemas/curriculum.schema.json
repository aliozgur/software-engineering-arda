{
  "$schema": "https://json-schema.org/draft/2020-12/schema",
  "title": "Apprenti Curriculum Manifest",
  "type": "object",
  "required": ["schemaVersion", "id", "name"],
  "properties": {
    "schemaVersion": {
      "type": "integer"
    },
    "id": {
      "type": "string"
    },
    "name": {
      "type": "string"
    },
    "sourceLocale": {
      "type": "string",
      "description": "BCP 47 language code of the canonical files. Default en. Do not reuse languages (programming languages).",
      "pattern": "^[a-z]{2,8}$"
    },
    "contentLocales": {
      "type": "array",
      "description": "Human locales this curriculum ships, including the source. Overlay files use sibling {stem}.{locale}.{ext}.",
      "items": {
        "type": "string",
        "pattern": "^[a-z]{2,8}$"
      },
      "uniqueItems": true
    },
    "progression": {
      "type": "string",
      "enum": ["sequential", "random"],
      "default": "random",
      "description": "sequential: an apprentice may not submit a later task while any earlier path task is still unsubmitted. random: tasks may be submitted in any order. Omitted field means random."
    }
  }
}
