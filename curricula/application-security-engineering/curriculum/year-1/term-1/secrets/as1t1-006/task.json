{
  "schemaVersion": 1,
  "id": "as1t1-006",
  "title": "Secrets That Never Belong in Git",
  "term": "year-1/term-1",
  "module": "secrets",
  "estimatedHours": 8,
  "objective": "Scan the repository you own for committed secrets, move real configuration to the environment, and add a check that fails when a documented dummy secret is planted.",
  "whyThisMatters": "The reference software-engineering path treats 'no secrets in Git' as an acceptance line on a design task. This task makes the control operational: history is scanned, names live in an example file, values do not, and a hook or CI job proves the scanner actually fails.",
  "competencies": [
    "security"
  ],
  "resources": [
    "github-actions"
  ],
  "instructionsFile": "instructions.md",
  "aiPolicy": {
    "mode": "guided",
    "allowedCapabilities": [
      "explain",
      "hint",
      "quiz"
    ],
    "solutionGenerationAllowed": false,
    "disclosureRequired": true
  },
  "evidence": [
    "A scan report (command, tool, and timestamp) covering the current tree and git history, with every finding marked rotated, removed, or false positive",
    "Committed .env.example (or equivalent) listing secret names only, plus .gitignore excluding the real secrets file",
    "Application code that reads secrets from the environment or an untracked local file, shown in the diff",
    "CI job or pre-commit hook configuration, plus logs showing it fail on a planted dummy secret and pass after the dummy is removed",
    "Reflection note answering the task questions"
  ],
  "acceptanceCriteria": [
    "A secret scan of the repository, including git history, is recorded; every finding is listed as rotated, removed, or false positive.",
    "Application secrets are read from the environment or a local untracked file; a committed example file lists names only.",
    ".gitignore excludes the real secrets file.",
    "A CI job or pre-commit hook fails when a planted dummy secret of a documented pattern is introduced, and passes once that dummy is removed.",
    "No live credential is committed in any commit on the submitted branch."
  ],
  "statusModel": [
    "not_started",
    "in_progress",
    "submitted",
    "revision_requested",
    "resubmitted",
    "approved"
  ]
}
